import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(theme => ({
  empty: {
    width: 300,
    height: 200,
    margin: "auto"
  },
  emptyText: {
    textAlign: "center"
  }
}));
